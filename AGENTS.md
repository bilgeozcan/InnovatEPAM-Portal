# Project Agent Notes

- Prefix shell commands with `rtk`.
- Keep the InnovatEPAM Portal MVP dependency-free unless a real requirement needs a framework.
- Run `rtk npm test` after meaningful behavior changes.
- Keep specs, ADRs, tests, and implementation aligned with `docs/prd.md` and `docs/stories.md`.
